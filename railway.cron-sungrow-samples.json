{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma generate && npx tsx scripts/collect-sungrow-samples.ts",
    "cronSchedule": "0 11,14,17,20,23 * * *",
    "restartPolicyType": "NEVER"
  }
}
