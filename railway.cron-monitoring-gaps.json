{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma generate && npx tsx scripts/backfill-monitoring-gaps.ts --apply --meses=3 --max-minutos=45",
    "cronSchedule": "0 8 * * *",
    "restartPolicyType": "NEVER"
  }
}
