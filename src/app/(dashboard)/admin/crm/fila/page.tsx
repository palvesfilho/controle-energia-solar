"use client";

import { FilaCrm } from "@/components/crm/fila-crm";

/** Fila do CRM no módulo Associação de Energia: só adesão de desconto na fatura. */
export default function FilaCrmAssociacaoPage() {
  return <FilaCrm modulo="assoc" />;
}
