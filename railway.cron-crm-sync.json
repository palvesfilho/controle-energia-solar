{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma generate && npx tsx scripts/run-crm-sync.ts",
    "cronSchedule": "0 16,22 * * *",
    "restartPolicyType": "NEVER"
  }
}
